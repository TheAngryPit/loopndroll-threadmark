# Progress

- Goal: `full product v1 working`
- Current state:
  - product/runtime seam fixed for the `thread_id` / `thread_name` schema
  - on-open canonical thread-name refresh is now wired into `ensureLoopndrollSetup()`
  - local desktop build is proven:
    - Bun-hosted renderer build: pass
    - `electrobun build`: pass
    - unsigned local app bundle launches and stays up long enough to complete setup
  - repo static checks are green from local binaries:
    - `oxlint`: pass
    - `oxfmt --check`: pass
    - `tsgo --noEmit -p tsconfig.json`: pass
  - focused + full Bun test suite: pass (`69 pass`, `0 fail`)
  - renderer production build is proven:
    - `node_modules/.bin/vite build`: pass
    - non-blocking warnings remain for large chunk size and `rolldown:vite-resolve` timing
  - passive app-server runtime smoke: pass
  - the launch-time refresh corrected stale stored thread names in the local product DB, including:
    - `C1` -> `Verificar loopndroll seguro`
    - `C4` -> `Iniciar investigação forense`
    - `C5` -> `Planeia setup local Open WebUI`
    - `C6` -> `Build freelancer pricing engine`
    - `C25` -> `Memory Writing Agent: Phase 2 (Consolidation)`
  - the product snapshot now exposes only the corrected visible rows:
    - `C1`, `C4`, `C5`, `C6`, `C25`, `C26`
  - fringe-case unresolved internal titles are now fail-closed at the snapshot boundary
    - raw DB artifacts may still exist
    - but internal prompt/instruction titles no longer surface in the supported product snapshot, even when a transcript path exists
  - hidden orphan thread artifacts are now pruned automatically
    - if a hidden internal-title row is still missing from canonical discovery for 3 setup/open passes
    - Loopndroll hard deletes the row and its dependent state by cascade
  - macOS app icon is now copied from the previously installed app into the build product
    - source: `/Applications/Loopndroll.app/Contents/Resources/AppIcon.icns`
    - versioned asset: `src/assets/AppIcon.icns`
    - bundle destination: `build/dev-macos-arm64/Loopndroll-dev.app/Contents/Resources/AppIcon.icns`
    - all three icon files have the same SHA-256
    - `Info.plist` points `CFBundleIconFile` at `AppIcon`
  - hook lifecycle management now distinguishes:
    - `running`, `paused`, and `stopped`
    - `inert now`, `removed from hooks.json`, and `unloaded from live runtime`
    - global `~/.codex/hooks.json` and known repo-local `<repo>/.codex/hooks.json`
  - Stop/Clear now use a fail-closed two-level policy:
    - if app-server activity is active or unknown, Loopndroll applies soft pause and records pending full removal
    - if app-server activity is idle, Loopndroll removes managed hooks and restarts the app-server lane to materialize unload
    - pending removal is retried automatically by setup/polling and the background monitor
  - managed hook runtime schema drift is now fixed:
    - generated hook still accepts Codex `input.session_id`
    - generated hook targets current DB columns `thread_id` and `thread_name`
    - `Start` has reinstalled the corrected live hook
  - live Telegram outbound proof now exists:
    - direct Bot API delivery: pass, Telegram `message_id=166`
    - managed `Stop -> loopndroll-hook -> Telegram` delivery: pass, Telegram `message_id=168`
    - runtime state remains `running`
    - no hook removal is pending
  - live Telegram inbound defect is now fixed at the app-server wake seam:
    - Telegram bridge had already consumed and queued an inbound user message
    - wake failed because app-server asynchronous notifications were being read as if they were RPC responses
    - the app-server client now ignores notification messages and waits for the matching RPC response id
    - the queued Telegram prompt was accepted by app-server and produced turn `019dbcb4-dc8a-7b20-b391-e997b23da0ef`
    - the queued prompt was consumed after acceptance
    - the rebuilt local app bundle is open and running the fixed code
    - Bot API check after restart found `pendingCount=0`, so no fresh automatic Telegram update remains to observe
  - pending hook-removal watcher singleton guard is implemented in source:
    - atomic lock path: `~/Library/Application Support/loopndroll/state/hook-removal-watch.lock`
    - lock records `pid`, `started_at`, `repo_root`, `hooks_path`, and `runtime_state_path`
    - second owner exits as `watcher already running` while the PID is alive
    - stale locks are replaced only after the recorded PID is no longer alive
    - shutdown cleanup covers `SIGTERM`, `SIGINT`, and normal `exit`
    - monitor scheduling uses backoff plus jitter and does not scan global process state in a loop
    - product snapshot/Settings now expose watcher active state and PID
    - this detour was not materialized by rebuilding/reopening the app and did not re-enable hooks/watchers
  - passive mode hook semantics are now fixed and live-installed:
    - generated managed hook now recognizes `passive` as a valid preset
    - generated managed hook now carries the passive Telegram footer
    - installed hook contains `value === "passive"` and the passive queue footer
    - installed hook still targets current `thread_id` / `thread_name` schema
    - live Stop synthetic proof sent Telegram `message_id=174`
    - passive Stop proof exited 0 with empty stdout/stderr, so passive did not auto-block the stop
    - passive delivery handler proof returned `Working on`, proving `passive reply -> queue -> app-server wake -> queue consumed`
    - app-server readback confirmed the target thread is readable with latest turn `019dbcd6-2d6e-79c3-9f55-dabca27d7136`
    - rebuilt app bundle is open and running the fixed code
    - post-relaunch installed hook still contains the passive normalizer/footer and current schema references
    - watcher singleton lock exists with the rebuilt app PID
  - Telegram inbound/app-server delivery audit is now corrected in source and rebuilt into the local app:
    - `reply-to` still resolves by the exact Telegram delivery receipt
    - `/reply Cx ...` still resolves by explicit thread ref
    - loose DM text now resolves to the latest prior passive thread that sent a Telegram notification in that chat
    - `Working on ...` now requires a fresh active thread observation after `turn/start`
    - spawned app-server transport is no longer closed immediately on accepted wake; it is kept alive until the started turn reaches a terminal status
    - keepalive now forwards app-server `item/completed` notifications so passive-mode final agent messages can be sent back to Telegram without relying on hooks
    - passive app-server output records Telegram delivery receipts so reply-to targeting remains tied to the correct thread
    - one-shot Telegram prompt state is preserved until `turn/completed`; if the app-server or bridge crashes mid-turn, the prompt is not silently lost
    - active target threads now use `turn/steer` with `expectedTurnId` instead of opening a competing `turn/start`
    - loose DM receipt fallback is now bounded to sessions whose effective preset is `passive`, including sessions inheriting `global_preset = passive`
    - non-passive modes are not targeted by that global/passive fallback path
    - runtime probe inserted a probe `userMessage` into C1 and completed turn `019dbcfa-cc56-70c1-b71c-aa9d56b34df1`
    - earlier interrupted-turn contradiction was explained by closing the spawned app-server transport too early
    - active-thread steer runtime probe completed turn `019dbd0e-904e-7e92-8d67-f03f99f49f9c` with final `agentMessage` text `STEER OK`
    - rebuilt local app bundle is open with launcher PID `70574` and main PID `70600`
  - app-server source audit:
    - current local Codex `0.124.0` exposes `stdio://`, `ws://IP:PORT`, and `off`
    - official OpenAI Codex app-server docs confirm the supported client flow is initialize, thread/resume, turn/start, stream notifications, turn/completed
    - local `unix://` / proxy control is not exposed by the installed `0.124.0` binary, so the current implementation uses a Loopndroll-owned `stdio://` app-server and reconnects by persisted `threadId`
- Strongest safe proof:
  - `code_proven`
  - `test_proven`
  - `runtime_proven`
- Strongest safe product truth:
  - local unsigned desktop/runtime proof exists for the non-Telegram-supported surfaces proved in Tranche I
  - Telegram outbound from the installed managed hook is now `runtime_proven`
  - Telegram inbound target insertion is now `runtime_proven`
  - app-server-managed passive completed-turn execution is now `runtime_proven`
  - active-thread `turn/steer` delivery is now `runtime_proven`
  - passive app-server output forwarding to Telegram is `code_proven` and `test_proven`
  - global passive loose-message targeting is `test_proven`
  - Telegram inbound completed-turn execution is not yet proven
  - hook-removal watcher singleton is `test_proven`
  - passive hook notification semantics are now `runtime_proven`
  - `full product v1 ready` is not claimed because Telegram-to-Codex-to-Telegram still needs a fresh external Telegram proof after the rebuild
  - shipped/released v1 is not claimed because signing, notarization, updater/feed publishing, and public distribution remain unproven
- Strongest contradiction still open:
  - fresh automatic Telegram inbound proof through the rebuilt running app is not captured because there are no pending Bot API updates after the current cursor
  - fresh external Telegram inbound proof through the rebuilt app is still not captured after the `turn/steer` fix; app-server completed-turn proof is now closed at runtime by direct managed-app-server probes
  - fresh external Bot API reply-to-passive-notification proof is not captured because Bot API returned `pendingCount=0` after passive notification `message_id=174`
  - shell-invoked repo-side setup remains `SQLITE_READONLY` for the real product DB on this machine, so DB write proof still has to go through the local app bundle path
- Next true executable task:
  - `Nanotask J.3e: Capture an external Telegram Bot API message through the rebuilt app and prove Telegram-to-Codex-to-Telegram output`
- Previous tranche:
  - `Tranche I: Local Product V1 Proof Closure`
  - progress: `100%` (`8/8` rows)
  - state: complete
- Active tranche:
  - `Tranche J: Product Readiness Proof`
  - progress: `96.7%` (`2.9/3` rows; passive/global receipt targeting narrowed, external Telegram end-to-end proof still open)
  - state: active
- Next program decision:
  - finish live Telegram inbound-output proof before any `ready` or release claim
